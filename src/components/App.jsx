import Statistics from './Statistics/Statistics';

export const App = () => {
  return <Statistics />;
};

//   return <Statistics good={} neutral={} bad={} total={} positivePercentage={}>.
// </Statistics>;
